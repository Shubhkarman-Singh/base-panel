<!doctype html>
<html class="h-full">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title><%= settings.name || 'Impulse' %> - Password Reset Error</title>
  <link href="../../assets/tailwind.css" rel="stylesheet">
  <link rel="preconnect" href="https://cdn.fontshare.com">
  <link href="https://api.fontshare.com/v2/css?f[]=general-sans@500,300,600,400,700&display=swap" rel="stylesheet">
</head>
<body style="font-family: 'General Sans'" class="bg-[#141414] h-full">
<div class="min-h-full flex">
  <div class="flex-1 flex flex-col justify-center py-12 px-4 sm:px-6 lg:flex-none lg:px-20 xl:px-24">
    <div class="mx-auto w-full max-w-sm lg:w-96">
      <div>
        <% if (settings.logo == true) { %>
           <% if (settings.logoType === 'link' && settings.logoLink) { %>
              <img class="h-12 w-auto rounded-xl" src="<%= settings.logoLink %>" onerror="this.src='/assets/default-logo.svg'">
           <% } else { %>
              <img class="h-12 w-auto rounded-xl" src="../../assets/logo.png" onerror="this.src='/assets/default-logo.svg'">
           <% } %>
        <% } %>
        <h2 class="<% if (settings.logo == true) { %>mt-6<% } %> text-3xl font-medium text-white">Password Reset Error</h2>
        <p class="mt-2 text-sm text-neutral-500">
          There was an issue with your password reset request.
        </p>
      </div>

      <div class="mt-8">
        <div class="bg-red-900/20 border border-red-500/30 rounded-xl p-4">
          <div class="flex">
            <div class="flex-shrink-0">
              <svg class="h-5 w-5 text-red-400" viewBox="0 0 20 20" fill="currentColor">
                <path fill-rule="evenodd" d="M10 18a8 8 0 100-16 8 8 0 000 16zM8.707 7.293a1 1 0 00-1.414 1.414L8.586 10l-1.293 1.293a1 1 0 101.414 1.414L10 11.414l1.293 1.293a1 1 0 001.414-1.414L11.414 10l1.293-1.293a1 1 0 00-1.414-1.414L10 8.586 8.707 7.293z" clip-rule="evenodd" />
              </svg>
            </div>
            <div class="ml-3">
              <h3 class="text-sm font-medium text-red-400">
                Password Reset Failed
              </h3>
              <div class="mt-2 text-sm text-red-300">
                <% if (error === 'Invalid token') { %>
                  <p>The password reset link is invalid. This could happen if:</p>
                  <ul class="mt-2 list-disc list-inside space-y-1">
                    <li>The link was copied incorrectly</li>
                    <li>The link has already been used</li>
                    <li>The link is from an old email</li>
                  </ul>
                <% } else if (error === 'Token expired') { %>
                  <p>The password reset link has expired. Reset links are only valid for 1 hour for security reasons.</p>
                <% } else if (error === 'Token already used') { %>
                  <p>This password reset link has already been used. Each reset link can only be used once for security.</p>
                <% } else { %>
                  <p>An error occurred while processing your password reset request. Please try again.</p>
                <% } %>
              </div>
            </div>
          </div>
        </div>

        <div class="mt-6">
          <a href="/auth/reset-password" class="w-full flex justify-center py-2 px-4 border border-transparent rounded-xl shadow-sm text-sm font-medium text-white bg-blue-600 hover:bg-blue-700 focus:outline-none focus:ring-2 focus:ring-offset-2 focus:ring-blue-500 transition">
            Request New Password Reset
          </a>
        </div>

        <div class="mt-6 text-center">
          <a href="/auth" class="text-sm text-neutral-500 hover:text-white transition">
            Back to Login
          </a>
        </div>
      </div>
    </div>
  </div>
  <div class="hidden lg:block relative w-0 flex-1">
    <img class="absolute inset-0 h-full w-full object-cover" src="/assets/bg.jpg" alt="">
  </div>
</div>
</body>
</html>